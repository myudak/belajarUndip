#include <stdio.h>
#include "math_operations.h"

int main() {
    int a = 5, b = 3;
    printf("Add: %d\n", add(a, b));
    printf("Subtract: %d\n", sub(a, b));
    printf("Multiply: %d\n", mul(a, b));
    return 0;
}
